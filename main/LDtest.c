#include "LivingDelusion.h"

//Testing functions when idk if my custom funcions work well or not (also testing other stuff)
void DoFuniHere(){
    #define OS_NAME "U" //Forgot #defined variables are constant by default :sob:
    RunThisAtStart();
    return;
}

int main(void){

    time_needed = clock();                              
    srand(time(NULL));                              
    setlocale(LC_ALL, "");                          
    system("title LIVING DELUSION [OPEN BETA 2]");

    DEBUG_MODE = true;

    DoFuniHere();

    return 0;
}
